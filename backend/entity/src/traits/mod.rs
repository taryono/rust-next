pub mod soft_delete;
// pub mod soft_delete_scope;

// pub use soft_delete::SoftDelete;
// pub use soft_delete_scope::SoftDeleteScope;

// pub type Result<T> = std::result::Result<T, Box<dyn std::error::Error>>;

// pub type HashMap<K, V> = std::collections::HashMap<K, V>;

// pub type Vec<T> = std::vec::Vec<T>;

// pub type Option<T> = std::option::Option<T>;

// pub type String = std::string::String;

// pub type BoxError = Box<dyn std::error::Error>;

// pub type BoxResult<T> = std::result::Result<T, BoxError>;

// pub type BoxFuture<T> = std::future::BoxFuture<'static, BoxResult<T>>;

// pub type BoxStream<T> = futures::stream::BoxStream<'static, BoxResult<T>>;

// pub type BoxTryStream<T> = futures::stream::BoxStream<'static, BoxResult<T>>;

// pub type BoxTryFuture<T> = futures::future::BoxFuture<'static, BoxResult<T>>;

// pub type BoxFutureStream<T> = futures::stream::BoxStream<'static, BoxResult<T>>;

// pub type BoxTryFutureStream<T> = futures::stream::BoxStream<'static, BoxResult<T>>;

// pub type BoxTryFutureOption<T> = futures::future::BoxFuture<'static, BoxResult<Option<T>>>;

// pub type BoxTryFutureVec<T> = futures::future::BoxFuture<'static, BoxResult<Vec<T>>>;

// pub type BoxTryFutureHashMap<K, V> = futures::future::BoxFuture<'static, BoxResult<HashMap<K, V>>>;

// pub type BoxTryFutureOptionHashMap<K, V> =
//     futures::future::BoxFuture<'static, BoxResult<Option<HashMap<K, V>>>>;

// pub type BoxTryFutureOptionVec<T> = futures::future::BoxFuture<'static, BoxResult<Option<Vec<T>>>>;

// pub type BoxTryFutureOptionString = futures::future::BoxFuture<'static, BoxResult<Option<String>>>;

// pub type BoxTryFutureString = futures::future::BoxFuture<'static, BoxResult<String>>;

// pub type BoxTryFutureBool = futures::future::BoxFuture<'static, BoxResult<bool>>;

// pub type BoxTryFutureOptionBool = futures::future::BoxFuture<'static, BoxResult<Option<bool>>>;

// pub type BoxTryFutureOptionU64 = futures::future::BoxFuture<'static, BoxResult<Option<u64>>>;

// pub type BoxTryFutureU64 = futures::future::BoxFuture<'static, BoxResult<u64>>;

// pub type BoxTryFutureOptionU32 = futures::future::BoxFuture<'static, BoxResult<Option<u32>>>;

// pub type BoxTryFutureU32 = futures::future::BoxFuture<'static, BoxResult<u32>>;

// pub type BoxTryFutureOptionU16 = futures::future::BoxFuture<'static, BoxResult<Option<u16>>>;

// pub type BoxTryFutureU16 = futures::future::BoxFuture<'static, BoxResult<u16>>;

// pub type BoxTryFutureOptionU8 = futures::future::BoxFuture<'static, BoxResult<Option<u8>>>;

// pub type BoxTryFutureU8 = futures::future::BoxFuture<'static, BoxResult<u8>>;

// pub type BoxTryFutureOptionI64 = futures::future::BoxFuture<'static, BoxResult<Option<i64>>>;

// pub type BoxTryFutureI64 = futures::future::BoxFuture<'static, BoxResult<i64>>;
